# Ambiente
APP_NAME="MinhaApp"
NODE_ENV=development
ENV=development
DEBUG=true
TZ=UTC

# Rede
HOST=0.0.0.0
PORT=3000
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:3000/api

# Chaves e segredos (substituir por valores reais em produção)
SECRET_KEY=change_me_generate_a_strong_secret
JWT_SECRET=change_me_generate_a_jwt_secret
SALT_ROUNDS=10

# Banco de dados (exemplo PostgreSQL)
# Formato DATABASE_URL: postgres://user:password@host:port/dbname
DATABASE_URL=postgres://dev_user:dev_password@localhost:5432/dev_db
# Ou variáveis separadas:
DB_HOST=localhost
DB_PORT=5432
DB_USER=dev_user
DB_PASSWORD=dev_password
DB_NAME=dev_db

# Redis (cache, sessões)
REDIS_URL=redis://localhost:6379/0

# Email (SMTP)
MAIL_HOST=smtp.mail.example.com
MAIL_PORT=587
MAIL_USER=your@account.example
MAIL_PASS=change_me
MAIL_FROM="noreply@your-domain.example"

# Observabilidade / logs / erros
LOG_LEVEL=debug
SENTRY_DSN=

# Flags de execução (ex.: em contêiner)
USE_DOCKER=false

# Notas:
# - Substitua todos os valores "change_me" por segredos reais no ambiente seguro.
# - Mantenha este arquivo no controle de versão apenas como exemplo (.env.example). O arquivo .env real não deve ser versionado.